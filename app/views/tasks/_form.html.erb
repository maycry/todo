<div class="create-task">
  <%= form_for(@task, remote: true) do |f| %>
    <%= f.hidden_field :project_id, value: project.id %>
    <div class="create-task-name">
      <%= f.text_field :name, placeholder: "What should be done?" %>   
    </div>
<!-- 
    <%= f.submit %>
     -->
  <% end %>
</div>