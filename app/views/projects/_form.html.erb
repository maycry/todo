<%= form_for(@project, remote: 'true') do |f| %>
  <div class="field">
    <%= f.text_field :name, placeholder: 'Enter title' %>
  </div>
   <div class="actions">
    <%= f.submit %>
  </div>
<% end %>