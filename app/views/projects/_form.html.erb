<div class="create-project-form">
<%= form_for(project, remote: 'true') do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div class="field">
    <%= f.text_field :name%>
  </div>
  <%= f.submit class: "notvis"  %>
<% end %>
</div>