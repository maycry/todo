<div class="project-header">
  <h1><%= project.name %></h1>
  <%= link_to "Edit", edit_project_path(project), remote: :true %>
  <%= link_to "Remove", project, method: :delete, remote: :true %>
</div>